PORT=3000

# JWT
JWT_SECRET=change_me
JWT_EXPIRES_IN=7d

# Database (SQLite by default)
DATABASE_URL="file:./dev.db"

# Database configuration
DATABASE_PROVIDER="postgresql"
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/itsm?schema=public"

# JWT and security
JWT_SECRET="change_me"
JWT_EXPIRES_IN="7d"

# App
PORT=3001
NODE_ENV=development

# LDAP / AD (optional)
LDAP_URL=
LDAP_BASE_DN=
LDAP_BIND_DN=
LDAP_BIND_PASSWORD=
